{
	"regions": ["gru1"],
	"version": 2,
	"buildCommand": "npx prisma generate && npm run build",
	"outputDirectory": "build",
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/main.ts",
			"methods": ["GET", "POST", "PUT", "DELETE"]
		}
	]
}
