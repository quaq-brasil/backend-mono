{
  "version": 2,
  "outputDirectory": "dist",
  "buildCommand": "npx prisma generate deploy && npm run build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH"]
    }
  ]
}
