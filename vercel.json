{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "buildCommand": "npx prisma generate && npm build",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}
