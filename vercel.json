{
	"regions": ["gru1"],
	"version": 2,
	"buildCommand": "pnpm prisma generate && pnpm build",
	"builds": [
		{
			"src": "src/main.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/main.ts",
			"methods": ["GET", "POST", "PUT", "DELETE"]
		}
	]
}
