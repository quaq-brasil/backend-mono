{
	"regions": ["gru1"],
	"version": 2,
	"buildCommand": "pnpm prisma generate && pnpm build",
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/main.ts",
			"methods": ["GET", "POST", "PUT", "DELETE"]
		}
	]
}
